;;; -*- lisp-data -*-
((:name "Run release"
        :command "odin run src -o:speed"
        :type "compile")
 (:name "Compile and run"
        :command ("mkdir -p build"
                  "odin run src -debug -out:build/debug")
        :type "compile"))